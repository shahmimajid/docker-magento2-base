box: php:7.1.20-fpm-jessie
## Build Definition
build:
  # Step that will be executed on build
  steps:
    # Test the project
    - script:
        name: Prepare for Magento Download
        code: |
           mkdir -p $HOME\.config\composer
           ls -ltrh $HOME\.config\composer
    - script:
        name: CMD
        code: pwd   